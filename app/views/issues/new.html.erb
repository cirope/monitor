<% @issue ||= issue %>

<div class="card">
  <div class="card-header">
    <h5><%= @title %></h5>
  </div>

  <div class="card-body">
    <%= render 'issues/form' %>
  </div>
</div>

<div class="mt-3">
  <%= link_to_index [@owner, :issues, filter: filter_query_hash] %>
</div>
