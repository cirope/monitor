+function () {
  <% if @panel %>
    $('[data-panel-id="<%= @panel.id %>"]').
      replaceWith('<%= j render('panel', panel: @panel) %>')
  <% else %>
    $('#new-panel').html('')
    $('[data-new-panel-btn]').removeAttr('hidden')
  <% end %>
}()
