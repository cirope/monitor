<div class="card">
  <div class="card-header">
    <h5>
      <%= @title %>

      <%= render 'tickets/new_owner' %>
    </h5>
  </div>

  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>

<div class="mt-3">
  <%= link_to_index scripts_path %>
</div>
