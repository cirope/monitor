<div class="panel panel-danger">
  <div class="panel-heading">
    <h4 class="panel-title">
      <%= link_to t('outputs.show.scripts_with_errors'), '#output_errors',
        data: { toggle: 'collapse' } %>
    </h4>
  </div>
  <div id="output_errors" class="panel-collapse collapse in">
    <div class="panel-body">
      <% exec.parse_and_find_lines_with_error.each do |script, errors| %>
        <h3><%= script %></h3>
        <ul>
          <% errors.each do |error| %>
            <li>
              <%= exec_output_error_line_with_link script, error, exec.still_valid? %>
            </li>
          <% end %>
        </ul>
      <% end %>
    </div>
  </div>
</div>
