<li class="tag-container" data-tagging-id="<%= tagging.id %>">
  <span class="label label-<%= tagging.tag.style %>"><%= tagging.tag %></span>
  <%= link_to_destroy [@issue, tagging], class: 'tag-controls small', data: { remote: true } %>
</li>
