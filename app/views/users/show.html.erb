<h1><%= @user %></h1>

<p>
  <%= mail_to @user.email %>
</p>

<p>
  <strong><%= roles_label %></strong>:
  <%= t "users.roles.#{@user.role}" %>
</p>

<hr>

<% unless ldap %>
  <%= link_to_edit [:edit, @user] %> |
<% end %>
<%= link_to_index users_path %>

<%= render 'role_help' %>
