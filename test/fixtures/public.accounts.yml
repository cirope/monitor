_fixture:
  model_class: Account
default:
  name: Default account
  tenant_name: default
  style: default
  debug_mode: true
  options: { 'token_interval': 1, 'token_frequency': 'months', 'cleanup_runs_after': 1, 'cleanup_executions_after': 1 }
