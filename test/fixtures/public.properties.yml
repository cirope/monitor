_fixture:
  model_class: Property

trace:
  key: Trace
  value: 'No'
  database: postgresql

postgresql_user:
  key: User
  value: <%= ENV['GH_ACTIONS'] ? 'postgres' : 'monitor' %>
  database: postgresql

postgresql_password:
  key: Password
  value: <%= ENV['GH_ACTIONS'] ? 'postgres' : 'monitor' %>
  database: postgresql

postgresql_database:
  key: Database
  value: monitor_test
  database: postgresql
